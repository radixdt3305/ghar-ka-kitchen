export enum UserRole {
  BUYER = "buyer",
  COOK = "cook",
  ADMIN = "admin",
  DELIVERY = "delivery",
}

export enum OtpPurpose {
  REGISTRATION = "registration",
  PASSWORD_RESET = "password_reset",
  LOGIN = "login",
}
